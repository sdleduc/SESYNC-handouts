## Configure git

system("git config --global user.name 'Steve LeDuc'")
system("git config --global user.email 'leduc.stephen@epa.gov'")

## Set a new origin URL

system('git remote set-url origin https://github.com/sdleduc/SESYNC-handouts.git')